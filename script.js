function LikeNinja(element) {
    let pulsador = ParseInd(element.innertext)
    count ++;
    element.innertext = count + "vistas"
}

function addlike(element) {
    let pulsador = parseInt(element.innertext)
    count ++ ;
    element.innertext = count + "vistas"
}